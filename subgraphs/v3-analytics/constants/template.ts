import {Address, BigInt} from '@graphprotocol/graph-ts'

export const MASTER_AGREEMENT_ADDRESS: Address = Address.fromString('{{ masterAgreementAddress }}')
export const FREE_MARGIN_UPGRADE_BLOCK: BigInt = BigInt.fromString('{{ freeMarginUpgradeBlock }}')
