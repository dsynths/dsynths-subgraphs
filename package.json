{
  "name": "dsynths-subgraphs",
  "private": true,
  "license": "MIT",
  "version": "1.0.0",
  "repository": "https://github.com/dsynths/dsynths-subgraphs",
  "workspaces": [
    "packages/*",
    "subgraphs/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "codegen": "yarn workspaces run codegen",
    "prepare:fusd": "yarn workspaces run prepare:fusd",
    "prepare:dei": "yarn workspaces run prepare:dei",
    "deploy:fusd": "yarn workspaces run deploy:fusd",
    "deploy:dei": "yarn workspaces run deploy:dei"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "@graphprotocol/graph-ts": "^0.27.0",
    "mustache": "^4.2.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.1"
  }
}
